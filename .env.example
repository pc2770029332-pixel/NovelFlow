# ===== NovelFlow 环境变量（可选，前端设置优先） =====

# Web 服务
WEB_HOST=127.0.0.1
WEB_PORT=8021

# 默认 LLM 配置（在界面里填更直观，这里只是兑底）
# DEFAULT_API_KEY=sk-xxx
# DEFAULT_BASE_URL=https://api.openai.com/v1
# DEFAULT_MODEL=gpt-4o-mini
